const { projetType } = require("./projetType");
const { tacheType } = require("./tacheType");
const { developpeurType } = require("./developpeurType");
const { reunionType } = require("./reunionType");

module.exports = {
    projetType,
    tacheType,
    developpeurType,
    reunionType
}